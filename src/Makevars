CXX_STD = CXX11
PKG_CXXFLAGS = -I../inst/include -Icore -Wno-everything
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

SOURCES = meanshift.cpp RcppExports.cpp $(wildcard core/*.cpp)
OBJECTS = $(SOURCES:.cpp=.o)

all: $(SHLIB)
$(SHLIB): $(OBJECTS)
